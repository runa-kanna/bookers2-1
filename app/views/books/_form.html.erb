<!-- 新規投稿フォーム -->
<H2>New Book</H2>
  <%= form_with model: book do |f| %>
    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Opnion</h4>
      <%= f.text_field :body %>
    <%= f.submit 'Create Book' %>
  <% end %>
