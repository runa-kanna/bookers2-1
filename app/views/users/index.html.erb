<h1>Users#index</h1>

<%= render 'info', user:  current_user %>
<%= render 'books/form', book: @book %>

<h1>ユーザー一覧</h1>

<% @users.each do |user| %>

<P>ユーザー画像</P>
<span>
  <%= image_tag user.get_profile_image, size: '50x50' %>
</span>
<p>名前</p>
<span><%= user.name %></span>
<% end %> 
